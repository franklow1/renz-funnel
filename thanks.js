/* =====================================================================
   RENZ STYLE SOLVE - FUNNEL LOADER
   Built 18 Sep 2026, 17:12. Do not edit this file by hand: it is generated by
   build-loader.py from the funnel source and overwritten on every build.

   The page that loads this needs one tag, once:

     <div id="rz-thanks-mount"></div>
     <script src="https://franklow1.github.io/renz-funnel/thanks.js" defer></script>

   Everything else lives here.
   ===================================================================== */
(function(){
  "use strict";
  /* a second copy of this tag on the same page would build a second quiz */
  var FLAG = "__RZ_LOADED_" + "rz_thanks_mount";
  if (window[FLAG]) return;
  window[FLAG] = true;

  var MOUNT_ID = "rz-thanks-mount";
  var CSS  = "\n/* the fragment's own boundary: a page builder's line-height and colour stop here */\n#rz-thanks{position:relative;isolation:isolate;text-align:left;font-size:16px;\n  line-height:normal;color:#15130F;letter-spacing:normal;word-spacing:normal}\n#rz-thanks *,#rz-thanks *:before,#rz-thanks *:after{box-sizing:border-box}\n#rz-thanks img{max-width:100%}\n#rz-thanks button{font-family:inherit}\n#rz-thanks .vid iframe,#rz-thanks .vid video,#rz-thanks .vid wistia-player{\n  position:absolute;inset:0;width:100%;height:100%;border:0;display:block}\n\n/* The builder wraps this in a section, a row and a column, each painting its own white\n   background with padding, which shows as a band around the paper. Painting behind them\n   cannot help, their white is opaque. So recolour the ancestors. :has is anchored to this\n   element, so only its own ancestors match, never a nav bar or a sibling. */\nhtml:has(#rz-thanks),body:has(#rz-thanks){background:#F4F1EA!important}\nbody:has(#rz-thanks) *:has(#rz-thanks){background:#F4F1EA!important}\n#rz-thanks *{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}\n#rz-thanks{\n --ink:#15130F; --graphite:#3D3A34; --pencil:#5C5750;\n --hairline:#D5CFC2; --rule2:#C6BEAC;\n --chalk:#F4F1EA; --canvas:#DFD6C0; --paper:#FBF9F3;\n --brass:#73572F; --brass-ink:#C79A5E; --brass-lift:#B58D57; --ink-rule:#3A352C; --chalk-soft:#CFC8B8;\n --done:#4A5D3A; --urgent:#8A1E38;\n --display:\"Newsreader\",\"Libre Caslon Text\",Georgia,serif;\n --text:\"Libre Caslon Text\",\"Times New Roman\",Georgia,serif;\n --micro:\"Cabin\",\"Gill Sans\",\"Gill Sans MT\",system-ui,sans-serif;\n --s1:6px; --s2:12px; --s3:18px; --s4:24px; --s5:36px; --s6:48px; --s7:72px; --s8:96px;\n --pad:24px; --wrap:620px;\n --ez:cubic-bezier(.2,.7,.2,1); --ezs:cubic-bezier(.16,.84,.24,1); --ezp:cubic-bezier(.2,1.1,.35,1);\n --ezdrape:cubic-bezier(.34,.02,.18,1); --ezcloth:cubic-bezier(.18,.72,.1,1.02);\n --t1:200ms; --t2:380ms; --t3:640ms;\n}\n#rz-thanks{background:var(--chalk);-webkit-text-size-adjust:100%;scroll-behavior:smooth}\n#rz-thanks{background:var(--chalk);color:var(--graphite);font:400 17px/29px var(--text);\n font-synthesis:none;orphans:2;widows:2;overflow-x:clip;\n background-image:repeating-linear-gradient(0deg,rgba(21,19,15,.03) 0 1px,transparent 1px 4px),\n                  repeating-linear-gradient(90deg,rgba(21,19,15,.022) 0 1px,transparent 1px 4px),\n                  repeating-linear-gradient(90deg,rgba(21,19,15,.018) 0 1px,transparent 1px 23px)}\n#rz-thanks .wrap{max-width:var(--wrap);margin:0 auto;padding:0 var(--pad)}\n#rz-thanks section{padding:var(--s7) 0}\n#rz-thanks .dark{background:var(--ink);color:var(--chalk-soft)}\n#rz-thanks .dark h2,#rz-thanks .dark h3,#rz-thanks .dark b{color:var(--chalk)}\n#rz-thanks .canvas{background:var(--canvas)}\n#rz-thanks h1{font-weight:600;font-size:clamp(32px,6vw,46px);line-height:1.27;letter-spacing:-.022em;color:var(--ink);\n font-family:var(--display);text-wrap:balance;max-width:15ch}\n#rz-thanks h2{font-weight:600;font-size:28px;line-height:33px;letter-spacing:-.014em;color:var(--ink);\n font-family:var(--display);text-wrap:balance}\n#rz-thanks h3{font-weight:600;font-size:20px;line-height:27px;letter-spacing:-.01em;color:var(--ink);font-family:var(--display)}\n#rz-thanks p{text-wrap:pretty}\n#rz-thanks b{font-weight:700;color:var(--ink)}\n#rz-thanks .eyeb{display:block;font:600 12px/16px var(--micro);letter-spacing:.22em;margin-right:-.22em;\n text-transform:uppercase;color:var(--brass);margin-bottom:var(--s3)}\n#rz-thanks .dark .eyeb{color:var(--brass-ink)}\n#rz-thanks .body{margin-top:var(--s3);max-width:34ch}\n#rz-thanks .mid{text-align:center;margin-left:auto;margin-right:auto}\n#rz-thanks .baste{display:block;height:5px;margin:0;border:0;opacity:.55;\n background-image:repeating-linear-gradient(90deg,\n   var(--brass) 0 9px, transparent 9px 15px, var(--brass) 15px 19px, transparent 19px 27px);\n background-size:27px 1px;background-repeat:repeat-x;background-position:0 2px;\n clip-path:inset(0 100% 0 0);transition:clip-path 900ms var(--ezdrape)}\n#rz-thanks .baste.in{clip-path:inset(0 0 0 0)}\n#rz-thanks .tick-head{padding-top:var(--s6)}\n#rz-thanks .ticket{display:flex;align-items:baseline;gap:var(--s2);padding-bottom:10px;\n border-bottom:max(1px,.055em) solid var(--ink)}\n#rz-thanks .ticket .k{font:600 10px/1 var(--micro);letter-spacing:.2em;text-transform:uppercase;color:var(--brass)}\n#rz-thanks .ticket .r{flex:1 1 auto;height:1px;background:var(--hairline)}\n#rz-thanks .ticket .no{font:400 13px/1 var(--micro);color:var(--pencil);letter-spacing:.08em;\n font-variant-numeric:tabular-nums lining-nums}\n#rz-thanks h1{margin-top:var(--s4)}\n#rz-thanks .sub{margin-top:var(--s3);max-width:32ch;color:var(--graphite)}\n#rz-thanks [hidden]{display:none!important}\n#rz-thanks .slot{margin-top:var(--s4);padding:var(--s3) 0;border-top:1px solid var(--hairline);border-bottom:1px solid var(--hairline);\n display:flex;align-items:baseline;justify-content:space-between;gap:var(--s3)}\n#rz-thanks .slot .lab{font:600 10px/1.4 var(--micro);letter-spacing:.2em;text-transform:uppercase;color:var(--brass)}\n#rz-thanks .slot .val{font-family:var(--display);font-weight:600;font-size:19px;line-height:26px;color:var(--ink);text-align:right}\n#rz-thanks .slot .val em{display:block;font-style:normal;font:400 13px/18px var(--micro);color:var(--pencil);margin-top:3px}\n#rz-thanks .again{margin-top:var(--s2);text-align:right}\n#rz-thanks .again a{font:400 13px/20px var(--micro);color:var(--pencil);text-decoration:underline;\n text-underline-offset:3px;text-decoration-color:var(--hairline)}\n#rz-thanks .again a:hover{color:var(--ink);text-decoration-color:var(--brass)}\n#rz-thanks .again a:focus-visible{outline:2px solid var(--brass);outline-offset:3px}\n#rz-thanks .vid{position:relative;margin-top:var(--s4);aspect-ratio:16/9;background:var(--ink);overflow:hidden;\n max-width:min(100%,calc(19svh * 16 / 9));margin-left:auto;margin-right:auto}\n#rz-thanks .vid.fitted{max-width:var(--vw-fit)}\n#rz-thanks .vid[hidden]{display:none!important}\n#rz-thanks .vid iframe,#rz-thanks .vid video{position:absolute;inset:0;width:100%;height:100%;border:0;display:block}\n#rz-thanks .vidph{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:14px}\n#rz-thanks .play{width:56px;height:56px;border:1px solid var(--brass-ink);border-radius:50%;position:relative}\n#rz-thanks .play:after{content:\"\";position:absolute;left:52%;top:50%;transform:translate(-50%,-50%);\n border-left:13px solid var(--brass-ink);border-top:8px solid transparent;border-bottom:8px solid transparent}\n#rz-thanks .phn{font:600 10px/1 var(--micro);letter-spacing:.2em;text-transform:uppercase;color:rgba(244,241,234,.66)}\n#rz-thanks .vlabel{position:absolute;top:14px;left:16px;right:16px;z-index:2;\n font:600 10px/1.5 var(--micro);letter-spacing:.18em;text-transform:uppercase;color:rgba(244,241,234,.58)}\n#rz-thanks .jobs{list-style:none;margin-top:var(--s5);border-top:max(1px,.055em) solid var(--ink)}\n#rz-thanks .jobs>li{border-bottom:1px solid var(--hairline);position:relative}\n#rz-thanks .jobs>li:last-child{border-bottom:max(1px,.055em) solid var(--ink)}\n#rz-thanks .job-top{display:grid;grid-template-columns:auto 1fr auto;align-items:start;gap:var(--s3);\n width:100%;padding:var(--s4) 0;background:none;border:0;cursor:pointer;text-align:left;font:inherit;color:inherit}\n#rz-thanks .job-top:focus-visible{outline:2px solid var(--brass);outline-offset:-3px}\n#rz-thanks .job-top:active{background:rgba(21,19,15,.045)}\n#rz-thanks .box{flex:0 0 auto;width:22px;height:22px;border:1.5px solid var(--rule2);position:relative;\n transition:border-color .26s var(--ez),background .26s var(--ez);margin-top:2px}\n#rz-thanks .box:after{content:\"\";position:absolute;left:4px;top:8px;width:11px;height:5px;\n border-left:2px solid var(--done);border-bottom:2px solid var(--done);\n transform:rotate(-45deg) scale(.4);opacity:0;\n transition:opacity .18s linear,transform .3s var(--ezp)}\n#rz-thanks li.done .box{border-color:var(--done)}\n#rz-thanks li.done .box:after{opacity:1;transform:rotate(-45deg) scale(1)}\n#rz-thanks .job-n{display:block;font:600 10px/1.4 var(--micro);letter-spacing:.2em;text-transform:uppercase;color:var(--brass);margin-bottom:5px}\n#rz-thanks .job-h{display:block;font-family:var(--display);font-weight:600;font-size:22px;line-height:29px;\n letter-spacing:-.012em;color:var(--ink);position:relative}\n#rz-thanks .job-h:after{content:\"\";position:absolute;left:-2px;right:-2px;top:.58em;height:1.5px;background:var(--brass);\n opacity:.65;transform:scaleX(0);transform-origin:0 50%;transition:transform .42s var(--ezs)}\n#rz-thanks li.done .job-h:after{transform:scaleX(1)}\n#rz-thanks li.done .job-h{color:var(--pencil)}\n#rz-thanks .job-when{display:block;margin-top:6px;font:600 9px/1.6 var(--micro);letter-spacing:.18em;text-transform:uppercase;color:var(--urgent)}\n#rz-thanks li.done .job-when{color:var(--done)}\n#rz-thanks .mk{position:relative;width:12px;height:12px;flex:0 0 auto;margin-top:9px;transition:transform .3s var(--ez)}\n#rz-thanks .mk:before,#rz-thanks .mk:after{content:\"\";position:absolute;left:0;top:5px;width:12px;height:1px;background:var(--brass)}\n#rz-thanks .mk:after{transform:rotate(90deg);transition:transform .3s var(--ez)}\n#rz-thanks li.open .mk:after{transform:rotate(0)}\n#rz-thanks .job-body{display:grid;grid-template-rows:0fr;transition:grid-template-rows .36s var(--ezs)}\n#rz-thanks li.open .job-body{grid-template-rows:1fr}\n#rz-thanks .job-body>div{overflow:hidden;min-height:0}\n#rz-thanks .job-in{padding:0 0 var(--s4) calc(22px + var(--s3));max-width:38ch}\n#rz-thanks .job-in p{font:400 16px/26px var(--text);color:var(--graphite);margin-bottom:var(--s2)}\n#rz-thanks .job-in p:last-child{margin-bottom:0}\n#rz-thanks .job-in .why{padding-left:var(--s3);border-left:1px solid var(--brass);color:var(--ink);font-style:italic}\n#rz-thanks .acts{display:flex;flex-wrap:wrap;gap:10px;margin-top:var(--s3)}\n#rz-thanks .act{display:inline-block;padding:11px 16px;border:1px solid var(--ink);background:none;cursor:pointer;\n font:600 11px/1.3 var(--micro);letter-spacing:.14em;text-transform:uppercase;color:var(--ink);\n text-decoration:none;transition:background .24s var(--ez),color .24s var(--ez)}\n#rz-thanks .act:hover{background:var(--ink);color:var(--chalk)}\n#rz-thanks .act:active{transform:scale(.97)}\n#rz-thanks .act.solid{background:var(--ink);color:var(--chalk)}\n#rz-thanks .act.solid:hover{background:var(--graphite)}\n#rz-thanks .act:focus-visible{outline:2px solid var(--brass);outline-offset:3px}\n#rz-thanks .act[hidden],#rz-thanks .acts[hidden]{display:none!important}\n#rz-thanks .tiny{display:block;margin-top:10px;font:400 14px/22px var(--text);color:var(--pencil)}\n#rz-thanks .prog{margin-top:var(--s4)}\n#rz-thanks .prog-r{display:flex;align-items:baseline;gap:12px;font:600 10px/1 var(--micro);\n letter-spacing:.18em;text-transform:uppercase;color:var(--pencil)}\n#rz-thanks .prog-r b{color:var(--ink);font-variant-numeric:tabular-nums;font-size:12px}\n#rz-thanks .prog-r span{flex:1 1 auto}\n#rz-thanks .prog-bar{margin-top:10px;height:2px;background:var(--hairline);overflow:hidden}\n#rz-thanks .prog-bar i{display:block;height:100%;width:0;background:var(--brass-lift);\n transition:width .7s var(--ezs)}\n#rz-thanks .doc{margin-top:var(--s5);padding:var(--s5) var(--s4) var(--s4);background:var(--paper);\n border:1px solid var(--rule2);position:relative}\n#rz-thanks .doc:before,#rz-thanks .doc:after{content:\"\";position:absolute;top:-5px;width:9px;height:9px;border-radius:50%;\n background:var(--brass);box-shadow:inset -1px -1px 0 rgba(21,19,15,.28)}\n#rz-thanks .doc:before{left:22px}\n#rz-thanks .doc:after{right:22px}\n#rz-thanks .doc-k{display:block;text-align:center;font:600 9px/1.5 var(--micro);letter-spacing:.2em;\n text-transform:uppercase;color:var(--pencil)}\n#rz-thanks .doc-n{display:block;text-align:center;font:italic 400 28px/1.1 var(--text);color:var(--ink);margin:var(--s2) 0 var(--s4)}\n#rz-thanks .doc-rows{list-style:none}\n#rz-thanks .doc-rows li{display:flex;align-items:baseline;gap:10px;padding:11px 0;border-bottom:1px solid var(--hairline)}\n#rz-thanks .doc-rows li:last-child{border-bottom:0}\n#rz-thanks .doc-rows span{flex:0 1 auto;min-width:0;font:600 9px/1.6 var(--micro);letter-spacing:.16em;text-transform:uppercase;color:var(--pencil)}\n#rz-thanks .doc-rows i{flex:1 1 auto;height:1px;border-bottom:1px dotted var(--rule2);transform:translateY(-3px)}\n#rz-thanks .doc-rows b{flex:0 1 auto;min-width:0;overflow-wrap:anywhere;max-width:18ch;font:600 9px/1.6 var(--micro);letter-spacing:.16em;text-transform:uppercase;\n color:var(--pencil);font-weight:600;transition:color .4s var(--ez)}\n#rz-thanks .doc-rows li.filled span{color:var(--ink)}\n#rz-thanks .doc-rows li.filled b{color:var(--done)}\n#rz-thanks .doc-rows li.filled i{border-bottom-style:solid;border-bottom-color:var(--brass-lift)}\n#rz-thanks .doc-cap{margin-top:var(--s4);padding-top:var(--s3);border-top:1px solid var(--hairline);\n font:400 15px/24px var(--text);color:var(--graphite)}\n#rz-thanks .doc-cap b{color:var(--ink)}\n#rz-thanks .leave{list-style:none;margin-top:var(--s4);border-top:1px solid var(--hairline)}\n#rz-thanks .leave li{display:flex;align-items:baseline;gap:14px;padding:var(--s3) 0;border-bottom:1px solid var(--hairline);\n font:400 17px/26px var(--text);color:var(--graphite)}\n#rz-thanks .leave li>span{flex:1 1 auto;min-width:0}\n#rz-thanks .leave li b{color:var(--ink)}\n#rz-thanks .leave li:before{content:\"\";flex:0 0 auto;width:18px;height:1px;background:var(--brass);transform:translateY(-6px)}\n#rz-thanks .gtee{margin:0 calc(var(--pad)*-1);padding:var(--s6) var(--s4);text-align:center;position:relative}\n#rz-thanks .seal{display:inline-block;padding:5px 12px;border:1px solid var(--brass-ink);\n font:600 9px/1.6 var(--micro);letter-spacing:.2em;text-transform:uppercase;color:var(--brass-ink)}\n#rz-thanks .gtee h2{margin-top:var(--s4);max-width:20ch;margin-left:auto;margin-right:auto}\n#rz-thanks .gtee .txt{margin:var(--s4) auto 0;max-width:32ch;font:400 16px/26px var(--text);color:var(--chalk-soft)}\n#rz-thanks .gtee .split{margin:var(--s5) auto 0;max-width:33ch;padding-top:var(--s4);border-top:1px solid var(--ink-rule)}\n#rz-thanks .gtee .split p{font:400 16px/26px var(--text);color:var(--chalk-soft)}\n#rz-thanks .gtee .split b{color:var(--chalk)}\n#rz-thanks .voice{margin-top:var(--s4);padding:var(--s4) 0;border-top:1px solid var(--ink-rule);border-bottom:1px solid var(--ink-rule)}\n#rz-thanks .voice p{font-family:var(--display);font-weight:600;font-size:23px;line-height:32px;letter-spacing:-.012em;\n color:var(--chalk);max-width:24ch}\n#rz-thanks .voice em{display:block;font-style:italic;font-weight:400;font-family:var(--text);\n font-size:18px;line-height:29px;color:var(--chalk-soft);margin-top:var(--s3)}\n#rz-thanks .recap{list-style:none;margin-top:var(--s4);display:flex;flex-wrap:wrap;gap:0;\n border-top:1px solid var(--ink-rule)}\n#rz-thanks .recap li{flex:1 1 33%;min-width:120px;padding:var(--s3) var(--s2) var(--s3) 0;\n border-bottom:1px solid var(--ink-rule);\n font:600 11px/1.5 var(--micro);letter-spacing:.16em;text-transform:uppercase;color:var(--chalk-soft)}\n#rz-thanks .recap li i{display:block;font-style:normal;color:var(--brass-ink);margin-bottom:6px;font-size:9px}\n#rz-thanks .recap li.done{color:var(--done)}\n#rz-thanks .foot{background:var(--chalk);border-top:1px solid var(--hairline);\n padding:var(--s6) var(--pad) calc(var(--s6) + env(safe-area-inset-bottom));text-align:center}\n#rz-thanks .foot p{font:400 14px/22px var(--text);color:var(--pencil);max-width:34ch;margin:0 auto 10px}\n#rz-thanks .cal{margin:var(--s5) auto 0;max-width:560px}\n#rz-thanks .cal-lab{display:block;text-align:center;font:600 11px/1 var(--micro);letter-spacing:.2em;\n text-transform:uppercase;color:var(--brass);margin:0 0 var(--s3)}\n#rz-thanks .cal-frame{position:relative;background:var(--paper);border:1px solid var(--hairline);\n box-shadow:0 1px 0 rgba(255,255,255,.5),0 22px 44px -32px rgba(21,19,15,.34);\n min-height:620px;overflow:hidden}\n#rz-thanks .cal-frame iframe{display:block;width:100%;min-height:620px;border:0}\n#rz-thanks .cal-load{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;\n font:400 15px/24px var(--micro);color:var(--pencil)}\n#rz-thanks .cal-note{text-align:center;margin:var(--s3) auto 0;max-width:34ch;\n font:400 14px/23px var(--micro);color:var(--pencil)}\n@media(max-width:520px){#rz-thanks .cal-frame,#rz-thanks .cal-frame iframe{min-height:700px}}\n#rz-thanks .folio{display:block;margin-top:var(--s7);text-align:center;font:400 11px/1 var(--micro);\n letter-spacing:.3em;color:var(--pencil);font-variant-numeric:tabular-nums}\n#rz-thanks .dark .folio{color:#857E75}\n#rz-thanks .rise{opacity:0;transform:translateY(16px)}\n#rz-thanks .rise.in{opacity:1;transform:none;transition:opacity var(--t3) var(--ezs),transform var(--t3) var(--ezs)}\n@media(max-height:730px){#rz-thanks .tick-head{padding-top:var(--s4)}\n#rz-thanks h1{font-size:clamp(28px,5.4vw,36px)}\n#rz-thanks .sub{margin-top:var(--s2)}\n#rz-thanks .slot{margin-top:var(--s3);padding:var(--s2) 0}\n#rz-thanks .vid{margin-top:var(--s2)}}\n@media(prefers-reduced-motion:reduce){#rz-thanks{scroll-behavior:auto}\n#rz-thanks *,#rz-thanks *:before,#rz-thanks *:after{animation:none!important;transition-duration:.001ms!important;transition-delay:0s!important}\n#rz-thanks .rise{opacity:1!important;transform:none!important}\n#rz-thanks .baste{clip-path:none!important}}\n#rz-thanks .tick-head .lede{margin-top:var(--s3);font-family:var(--display);font-weight:400;\n  font-size:clamp(17px,2.4vw,21px);line-height:1.5;color:var(--graphite);max-width:34ch}\n#rz-thanks .tick-head .lede b{color:var(--ink);font-weight:600}\n#rz-thanks .tick-head .sub{margin-top:var(--s2);font-size:15px;line-height:23px;color:var(--pencil);max-width:38ch}";
  var HTML = "<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>\n<link rel=\"stylesheet\" href=\"https://fonts.googleapis.com/css2?family=Newsreader:ital,wght@0,500;0,600;1,400&family=Libre+Caslon+Text:ital,wght@0,400;0,700;1,400&family=Cabin:wght@400;500;600&display=swap\">\n\n<div id=\"rz-thanks\"><!-- ====================== BOOKED ====================== -->\n<section class=\"tick-head\">\n <div class=\"wrap\">\n  <div class=\"ticket\"><span class=\"k\">Renz Tailors</span><i class=\"r\"></i><span class=\"no\" id=\"ticket-no\">Paid</span></div>\n  <h1 id=\"h1\">Right. You&rsquo;re In.</h1>\n  <p class=\"lede\" id=\"lede\">The call is paid for.<br>One thing left, and it takes ten seconds.<br><b>Pick your time, just under this video.</b></p>\n  <p class=\"sub\" id=\"sub\">Most men mean to sort this out and never do. You just did.</p>\n\n  <div class=\"vid\" id=\"vid\">\n   <div class=\"vidph\" id=\"vidph\"><div class=\"play\"></div><span class=\"phn\">Video goes here</span></div></div>\n\n  <!-- Booked already? show the time. Not yet? show the calendar. Never both. -->\n  <div class=\"slot\" id=\"slot\" hidden>\n   <span class=\"lab\">Your call</span>\n   <span class=\"val\" id=\"slot-val\">Confirmed<em id=\"slot-sub\">Forty five minutes, one to one</em></span>\n  </div>\n  <p class=\"again\" id=\"again\" hidden><a href=\"#\" id=\"again-a\">Need to move it?</a></p>\n\n  <div class=\"cal\" id=\"cal\">\n   <span class=\"cal-lab\">Pick your time</span>\n   <div class=\"cal-frame\"><div class=\"cal-load\" id=\"cal-load\">Loading the calendar&hellip;</div></div>\n   <p class=\"cal-note\">Forty five minutes, one to one. Nothing more to pay.</p>\n  </div>\n\n </div>\n</section>\n\n<!-- ====================== WHAT HE JUST DID ====================== -->\n<i class=\"baste\" aria-hidden=\"true\"></i>\n<section>\n <div class=\"wrap rise\">\n  <span class=\"eyeb\">What you just did</span>\n  <h2>Most men are still thinking about it.</h2>\n  <p class=\"body\">You have been meaning to sort your clothes out for years. So has every man you\n   know. Ten minutes ago you stopped meaning to.</p>\n  <ul class=\"leave\">\n   <li><span>You are not walking round the shops again <b>guessing.</b></span></li>\n   <li><span>You are not taking the word of <b>a lad on commission.</b></span></li>\n   <li><span>You are not waiting until you have <b>lost half a stone.</b></span></li>\n  </ul>\n  <p class=\"body\">Your colours, your cuts and your numbers, written down once by a man who does\n   this for a living. <b>Then you use them for the rest of your life.</b></p>\n  <span class=\"folio\">1</span>\n </div>\n</section>\n\n<!-- ====================== THE THREE THINGS ====================== -->\n<i class=\"baste\" aria-hidden=\"true\"></i>\n<section class=\"canvas\">\n <div class=\"wrap rise\">\n  <span class=\"eyeb\">Here&#8217;s what happens now</span>\n  <h2>Four things. Three of them take five minutes.</h2>\n  <p class=\"body\">Tick them off as you go. This page remembers.</p>\n\n  <ol class=\"jobs\" id=\"jobs\">\n   <li id=\"job-book\">\n    <button type=\"button\" class=\"job-top\" data-open aria-expanded=\"false\">\n     <span class=\"box\" role=\"checkbox\" aria-checked=\"false\" data-tick tabindex=\"0\" aria-label=\"Mark done: pick your time\"></span>\n     <span><span class=\"job-n\">One</span><span class=\"job-h\">Pick your time.</span><span class=\"job-when\">Ten seconds</span></span>\n     <span class=\"mk\" aria-hidden=\"true\"></span></button>\n    <div class=\"job-body\"><div><div class=\"job-in\">\n     <p id=\"job-book-p\">The calendar is at the top of this page. <b>Nothing else starts until your time is in.</b></p>\n     <p>Somewhere quiet, on a laptop or a phone with a camera. If you can, within reach of your closet, because we are going to run those four cuts on your real clothes.</p>\n     <p class=\"tiny\">Can't be near it? Send me a few photos of the inside of it and we'll work from those.</p>\n     <span class=\"acts\"><a class=\"act solid\" data-tocal href=\"#\">Take me to the calendar</a></span>\n    </div></div></div>\n   </li>\n\n   <li id=\"job-email\">\n    <button type=\"button\" class=\"job-top\" data-open aria-expanded=\"false\">\n     <span class=\"box\" role=\"checkbox\" aria-checked=\"false\" data-tick tabindex=\"0\" aria-label=\"Mark done: check your email\"></span>\n     <span><span class=\"job-n\">Two</span><span class=\"job-h\">Check your email.</span><span class=\"job-when\">Right now</span></span>\n     <span class=\"mk\" aria-hidden=\"true\"></span></button>\n    <div class=\"job-body\"><div><div class=\"job-in\">\n     <p>There&#8217;s a message from me with three things in it. <b>The instructions for the scan. The photos I need. And your invite to The Fitting Room.</b></p>\n     <p>You&#8217;re in there from today. The link is in that message. Go in, say hello, and have a look round. I&#8217;m in there every day.</p>\n     <p class=\"tiny\">Not there? Check spam. Still not there? Message me and I&#8217;ll send it again.</p>\n    </div></div></div>\n   </li>\n\n   <li id=\"job-scan\">\n    <button type=\"button\" class=\"job-top\" data-open aria-expanded=\"false\">\n     <span class=\"box\" role=\"checkbox\" aria-checked=\"false\" data-tick tabindex=\"0\" aria-label=\"Mark done: do the scan and send the photos\"></span>\n     <span><span class=\"job-n\">Three</span><span class=\"job-h\">Do the scan. Send the photos.</span><span class=\"job-when\">Today. Not tonight.</span></span>\n     <span class=\"mk\" aria-hidden=\"true\"></span></button>\n    <div class=\"job-body\"><div><div class=\"job-in\">\n     <p>The scan takes about two minutes. It gets your measurements. The photos are your face, your hands and your skin. They get your colours.</p>\n     <p class=\"why\">Everything you get on that call comes off those two things. Your colours. Your measurements. The cuts that work on your body, and the ones to stop buying for good.</p>\n     <p><b>That&#8217;s your style guide, and I build it before we speak.</b> If the scan and the photos aren&#8217;t in, I&#8217;m walking in blind. And you&#8217;re paying for a call where I&#8217;m guessing. Neither of us wants that.</p>\n     <p><b>The scan link is in that same message.</b> So do it now. Then it&#8217;s done, and you never think about it again.</p>\n    </div></div></div>\n   </li>\n\n   <li id=\"job-cal\">\n    <button type=\"button\" class=\"job-top\" data-open aria-expanded=\"false\">\n     <span class=\"box\" role=\"checkbox\" aria-checked=\"false\" data-tick tabindex=\"0\" aria-label=\"Mark done: add the call to your calendar\"></span>\n     <span><span class=\"job-n\">Four</span><span class=\"job-h\">Put the call in your calendar.</span><span class=\"job-when\">While you&#8217;re here</span></span>\n     <span class=\"mk\" aria-hidden=\"true\"></span></button>\n    <div class=\"job-body\"><div><div class=\"job-in\">\n     <p>Forty five minutes. I&#8217;ll remind you the day before and an hour before. <b>Add it yourself now, while you&#8217;re here.</b></p>\n     <p>Take it somewhere quiet, on a laptop or a phone with a camera. And if you can, <b>within reach of your closet</b>. We are going to run the four cuts on your real clothes, and it is easier if you can grab them.</p>\n     <p class=\"tiny\">Can&#8217;t be near it? Send me a few photos of the inside and we&#8217;ll work from those.</p>\n     <span class=\"acts\"><a class=\"act solid\" data-cal href=\"#\">Add to calendar</a><a class=\"act\" id=\"cal-file\" href=\"#\" hidden>Apple or Outlook</a></span>\n    </div></div></div>\n   </li>\n  </ol>\n\n  <div class=\"prog\">\n   <div class=\"prog-r\"><b id=\"pg-n\">0</b><span>of <i id=\"pg-d\" style=\"font-style:normal\">four</i> done</span><b id=\"pg-left\">4</b><span style=\"flex:0 0 auto\">to go</span></div>\n   <div class=\"prog-bar\"><i id=\"pg-bar\"></i></div>\n  </div>\n  <span class=\"folio\">2</span>\n </div>\n</section>\n\n<!-- ====================== THE DOCUMENT ====================== -->\n<i class=\"baste\" aria-hidden=\"true\"></i>\n<section>\n <div class=\"wrap rise\">\n  <span class=\"eyeb\">What I&#8217;m building while you wait</span>\n  <h2>By the time you sit down, I&#8217;ll have already seen you.</h2>\n  <div class=\"doc\">\n   <span class=\"doc-k\">Style guide</span>\n   <span class=\"doc-n\" id=\"doc-name\">Your style guide</span>\n   <ul class=\"doc-rows\" id=\"doc-rows\">\n    <li data-need=\"scan\"><span>Shoulder</span><i></i><b>waiting on the scan</b></li>\n    <li data-need=\"scan\"><span>Chest</span><i></i><b>waiting on the scan</b></li>\n    <li data-need=\"scan\"><span>Waist, arm, leg</span><i></i><b>waiting on the scan</b></li>\n    <li data-need=\"scan\"><span>Your colours</span><i></i><b>waiting on the photos</b></li>\n    <li data-need=\"scan\"><span>What suits your face</span><i></i><b>waiting on the photos</b></li>\n    <li data-need=\"call\"><span>Cuts built for you</span><i></i><b>on the call</b></li>\n    <li data-need=\"call\"><span>What to keep, what to lose</span><i></i><b>on the call</b></li>\n    <li data-need=\"call\"><span>What to wear to what</span><i></i><b>on the call</b></li>\n   </ul>\n   <p class=\"doc-cap\" id=\"doc-cap\">Nothing in it yet. <b>The scan is what starts it.</b></p>\n  </div>\n\n  <span class=\"eyeb\" style=\"margin-top:var(--s6)\">What you leave with</span>\n  <ul class=\"leave\">\n   <li><span><b>What to keep, what to lose, and what to buy next.</b></span></li>\n   <li><span><b>What to wear to what.</b></span></li>\n   <li><span>Your style guide. Written down. <b>Yours forever.</b></span></li>\n  </ul>\n  <p class=\"body\" id=\"heads-up\"><!-- INVENTED, LORENZO TO CONFIRM -->One honest heads up, so nothing surprises you. At the end, I&#8217;ll show you what the full thirty three looks like for you. Not for most men. For you.</p>\n  <span class=\"folio\">3</span>\n </div>\n</section>\n\n<!-- ====================== THE GUARANTEE ====================== -->\n<i class=\"baste\" aria-hidden=\"true\"></i>\n<section class=\"dark\">\n <div class=\"wrap rise\">\n  <div class=\"gtee\">\n   <span class=\"seal\">The Straight Back Guarantee</span>\n   <h2>It still stands. You just have to turn up.</h2>\n   <p class=\"txt\">If you&#8217;re on the call and you don&#8217;t feel it was worth it, tell me, and <b>every penny comes straight back</b>. The call, and anything you added to it.</p>\n   <div class=\"split\">\n    <p>The one thing it does not cover is missing it. <b>If you don&#8217;t turn up, there&#8217;s nothing for you to judge, so there&#8217;s nothing to refund.</b> And I&#8217;ll have already built your style guide.</p>\n    <p style=\"margin-top:var(--s3)\">Something comes up? Message me at least a day before and we&#8217;ll move it. That&#8217;s fine. <b>Just don&#8217;t vanish.</b></p>\n   </div>\n  </div>\n  <span class=\"folio\">4</span>\n </div>\n</section>\n\n<!-- ====================== THE VOICE ====================== -->\n<i class=\"baste\" aria-hidden=\"true\"></i>\n<section class=\"dark\">\n <div class=\"wrap rise\">\n  <span class=\"eyeb\">One more thing</span>\n  <h2>Why do most men who book something like this not turn up?</h2>\n  <p class=\"body\">It&#8217;s not that they&#8217;re busy.</p>\n  <div class=\"voice\">\n   <p>It&#8217;s that somewhere between now and then, the closet talks them out of it.\n    <em>It&#8217;s fine. It&#8217;s close enough. I&#8217;ll sort it another time.</em></p>\n  </div>\n  <p class=\"body\">You&#8217;ve heard that voice. <b>It&#8217;s the voice that got you here.</b></p>\n  <p class=\"body\">So do two things right now, while you&#8217;re still the man who booked. Put the call in your calendar. And do the scan.</p>\n  <p class=\"body\"><b>Once the scan&#8217;s in, you&#8217;re in.</b> Your style guide is being built. You&#8217;ll turn up.</p>\n\n  <ul class=\"recap\" id=\"recap\">\n   <li data-for=\"job-email\"><i>One</i>Email</li>\n   <li data-for=\"job-scan\"><i>Two</i>Scan and photos</li>\n   <li data-for=\"job-cal\"><i>Three</i>Calendar</li>\n  </ul>\n  <p class=\"body\" style=\"margin-top:var(--s5)\">You&#8217;re already in The Fitting Room, so I&#8217;ll see you in there before I see you on the call.</p>\n  <span class=\"folio\">5</span>\n </div>\n</section>\n\n<div class=\"foot\">\n <p>Renz Tailors &middot; London</p>\n <p>Something wrong with any of this? Reply to your receipt and I&#8217;ll sort it.</p>\n</div></div>";

  /* ------------------------------------------------------------------
     WHERE THIS PAINTS, AND WHEN

     The host is a server rendered Vue app, and the div we are meant to
     paint into is part of the markup that app is about to take ownership
     of. Write into it too early and the hand over fails. What dies is
     their order form: no total, no card field, no way to pay, and not one
     line in the console to say so.

     Waiting for their app would mean holding a blank page in front of a
     man who just clicked an advert, so we do not wait to paint. The funnel
     is painted at once into a sheet of its own, laid over the page, which
     is exactly what the quiz looks like anyway. Then, once their checkout
     has actually drawn itself, the sheet is lifted into place in the page
     and the cover comes off. He sees none of it.

     On a page with no host app there is nothing to wait for and nothing to
     move: it paints where it is told, as it always did.
     ------------------------------------------------------------------ */
  var HOST_ID   = 'rz-host';
  var HOST_ROOT = '__nuxt';    /* the element their app mounts on */
  var SETTLE    = 700;         /* a page with no checkout on it still gets a moment */
  var LIMIT     = 12000;       /* never leave the funnel floating over their page for good */
  /* Moving the sheet into the page while the host is still building its checkout is what
     emptied the order form and left it at zero pounds with no card field. On a page that
     has a checkout the wait is long, because there is nothing to lose by waiting: the man
     can already see and use the funnel while it floats. The measured time for the checkout
     to come alive is two to four seconds, so thirty is a lot of room for a bad line. */
  var ORDER_LIMIT = 30000;

  function hostRoot(){ return document.getElementById(HOST_ROOT); }
  function mountDiv(){ return document.getElementById(MOUNT_ID); }

  /* their app is up AND the piece of it that we can break is finished */
  var appSeen = 0;
  function hostReady(){
    var r = hostRoot();
    if (!r) return true;
    if (!r.__vue_app__) return false;
    if (!appSeen) appSeen = Date.now();
    var order = document.querySelector('.c-order, [id^="one-step-order-"]');
    if (order) return !!document.querySelector(
      '.payment-content section, iframe[src*="stripe"], .form-btn');
    return Date.now() - appSeen > SETTLE;
  }

  function sheet(){
    if (document.getElementById('rz-host-css')) return;
    var st = document.createElement('style');
    st.id = 'rz-host-css';
    st.appendChild(document.createTextNode(
      '#' + HOST_ID + '.rz-float{position:fixed;top:0;right:0;bottom:0;left:0;' +
      'z-index:2147483000;overflow-y:auto;-webkit-overflow-scrolling:touch;' +
      'background:#F4F1EA}'));
    (document.head || document.documentElement).appendChild(st);
  }

  /* The host page paints its own content, and its order form, in the second before this
     file has even finished downloading. A man saw a checkout he had not asked for, and a
     white page, and could scroll away from a quiz that had not arrived yet.

     So the paste snippet carries an opaque sheet of paper as inline CSS, which the browser
     honours on the first paint. This takes it away the moment there is a funnel underneath
     it instead. If this file never arrives, the sheet stays, which is the right failure:
     a blank page beats somebody's half drawn checkout. */
  function unveil(){
    try{
      var v = document.getElementById('rz-veil');
      if (v && v.parentNode) v.parentNode.removeChild(v);
      var vs = document.getElementById('rz-veil-css');
      if (vs && vs.parentNode) vs.parentNode.removeChild(vs);
    }catch(e){}
  }

  function paint(){
    try{
      var st = document.createElement('style');
      st.setAttribute('data-rz-funnel','1');
      st.appendChild(document.createTextNode(CSS));
      (document.head || document.documentElement).appendChild(st);
      sheet();
      var host = document.getElementById(HOST_ID);
      if (!host){
        host = document.createElement('div');
        host.id = HOST_ID;
      }
      if (hostRoot() && mountDiv()){
        /* their page: start over it, and go into the flow when it is safe */
        (document.body || document.documentElement).appendChild(host);
        host.className = 'rz-float';
      } else if (!host.parentNode){
        /* a bare page: where the tag sits, or failing that the end of the body */
        var m = mountDiv();
        if (m) m.appendChild(host);
        else {
          var sc = document.currentScript;
          if (sc && sc.parentNode) sc.parentNode.insertBefore(host, sc.nextSibling);
          else (document.body || document.documentElement).appendChild(host);
        }
      }
      host.innerHTML = HTML;
    }catch(e){
      if (window.console && console.error) console.error('[RZ] could not paint the funnel', e);
      return false;
    }
    return true;
  }

  /* lift the sheet into the page, keeping his place in it */
  function home(){
    var m = mountDiv(), h = document.getElementById(HOST_ID);
    /* Nothing to move is still settled. Returning quietly here once left anything waiting
       on the handover sat there until the fifteen second fallback fired. */
    if (!m || !h || h.parentNode === m) { settled(); return; }
    var inner = 0;
    try{ inner = h.scrollTop || 0; }catch(e){}
    m.appendChild(h);
    h.className = '';
    try{
      if (inner > 0){
        var top = Math.round(h.getBoundingClientRect().top + window.pageYOffset);
        window.scrollTo(0, top + inner);
      }
    }catch(e){}
    /* the quiz measures the room it has, and it has just been given a different one */
    try{ if (window.RZ && RZ.fitQuiz) RZ.fitQuiz(); }catch(e){}
    settled();
  }

  /* Moving this subtree re-parents everything in it, and a video player that has already
     wired itself to a node loses that node and refuses to start. Wistia says so out loud:
     "Could not find element in DOM. Failed to initialize video." So nothing that embeds
     anything is built until the funnel has stopped moving. */
  function settled(){
    if (window.__rzSettled) return;
    window.__rzSettled = true;
    try{ window.dispatchEvent(new Event('rz:settled')); }catch(e){}
  }
  window.__rzWhenSettled = function(fn){
    if (window.__rzSettled) { try{ fn(); }catch(e){} return; }
    var done = false;
    function go(){ if (done) return; done = true; try{ fn(); }catch(e){} }
    window.addEventListener('rz:settled', go, {once:true});
    /* a page that never settles still has to show its film */
    setTimeout(function(){ settled(); go(); }, 15000);
  };

  /* Wait for the host's checkout to finish drawing, then take the funnel out of its own
     floating sheet and put it into the page. Anything that wants to happen after that
     hands in a callback. */
  function handover(done){
    if (!hostRoot() || !mountDiv()) { settled(); if (done) done(); return; }
    var t0 = Date.now();
    (function poll(){
      var hasOrder = false;
      try{ hasOrder = !!document.querySelector('.c-order, [id^="one-step-order-"]'); }catch(e){}
      var budget = hasOrder ? ORDER_LIMIT : LIMIT;
      if (hostReady() || Date.now() - t0 > budget) { home(); if (done) done(); return; }
      setTimeout(poll, 40);
    })();
  }
  window.__rzHandover = handover;

  function start(){
    if (!paint()) return;
    /* the funnel's own code, verbatim, with the markup already on the page */
    (function(){
    'use strict';
    var ROOT=null;
    function root(){ return ROOT || (ROOT=document.getElementById('rz-thanks')); }
    var RZT={
      id  : function(x){ var r=root(); return r?r.querySelector('#'+x):null; },
      one : function(x){ var r=root(); return r?r.querySelector(x):null; },
      all : function(x){ var r=root(); return r?r.querySelectorAll(x):[]; }
    };
    /* a page with no charset decodes this file as Latin-1 and the script dies, so the
       fragment is pure ASCII; a page with no viewport meta lays out at 980px, so add one */
    try{
      if(!document.querySelector('meta[charset], meta[http-equiv="Content-Type"]')){
        var mc=document.createElement('meta'); mc.setAttribute('charset','utf-8');
        (document.head||document.documentElement).insertBefore(mc,(document.head||document.documentElement).firstChild);
      }
      if(!document.querySelector('meta[name="viewport"]')){
        var mv=document.createElement('meta'); mv.name='viewport';
        mv.content='width=device-width,initial-scale=1,viewport-fit=cover';
        (document.head||document.documentElement).appendChild(mv);
      }
    }catch(e){}

    /* ---------- the film. paste the whole Wistia snippet or just the media id ---------- */
    var THANKS_WISTIA = '<wistia-player media-id="7ehs06hu2p" aspect="1.5434083601286173"></wistia-player>';
    try{(function(){
      var raw=String(THANKS_WISTIA||'').trim(); if(!raw) return;
      var build=function(){
      var m=raw.match(/media-id=["']([A-Za-z0-9]+)["']/)
          || raw.match(/medias\/([A-Za-z0-9]+)/)
          || raw.match(/embed\/([A-Za-z0-9]+)\.js/)
          || (/^[A-Za-z0-9]+$/.test(raw) ? [null, raw] : null);
      if(!m) return;
      var id=m[1];
      var am=raw.match(/aspect=["']([0-9.]+)["']/);
      var aspect=am?parseFloat(am[1]):0;
      var vid=RZT.id('vid'); if(!vid) return;
      var ph=RZT.id('vidph'); if(ph) ph.parentNode.removeChild(ph);
      vid.hidden=false;
      if(aspect>0.2&&aspect<5){ vid.style.aspectRatio=String(aspect); }
      function js(src,mod){ var t=document.createElement('script'); t.src=src; t.async=true;
        if(mod) t.type='module'; document.head.appendChild(t); }
      js('https://fast.wistia.com/player.js',false);
      js('https://fast.wistia.com/embed/'+id+'.js',true);
      var pl=document.createElement('wistia-player');
      pl.setAttribute('media-id',id);
      if(aspect) pl.setAttribute('aspect',String(aspect));
      vid.appendChild(pl);
      };
      /* the loader is about to lift this stage into the page, and a player built before that
         move loses the node it wired itself to */
      if(typeof window.__rzWhenSettled === 'function') window.__rzWhenSettled(build);
      else build();
    })();}catch(e){}

    (function(){
    'use strict';
    /* ===================================================================== FILL THESE IN */
    /* The Fitting Room, the scan and the way to reach him all arrive by email, so this page
       does not carry a link to any of them. Three buttons here would be three more things to
       keep in step with whatever the email actually says. */
    var VIDEO            = { iframe:'', file:'', poster:'' };
    /* the booked slot. the booking platform can pass ?when=2026-09-22T15:30 and ?tz=Europe/London,
       otherwise the page says "in your calendar" and never invents a time. */
    /* ===================================================================== */

    var $=function(id){ return RZT.id(id); };
    var RM=false; try{ RM=matchMedia('(prefers-reduced-motion:reduce)').matches; }catch(e){}
    function qp(k){ try{ return new URLSearchParams(location.search).get(k); }catch(e){ return null; } }

    /* The stub used to read "Order 0201" for every single buyer. A made up number on a receipt
       is a small lie that two men comparing notes would catch, so it now says "Paid" unless
       the checkout actually hands us an order id. */
    function paintTicket(){
      var el=RZT.id('ticket-no'); if(!el) return;
      var id = qp('order_id')||qp('orderId')||qp('transaction_id')||qp('payment_id');
      if(!id) return;
      id=String(id).replace(/[^A-Za-z0-9]/g,'');
      if(id.length<4) return;
      el.textContent='Order '+id.slice(-6).toUpperCase();
    }

    /* ---------- reveals first, so nothing can ever be left invisible ---------- */
    try{
      var rises=[].slice.call(RZT.all('.rise'));
      if(RM||!('IntersectionObserver' in window)){ rises.forEach(function(e){ e.classList.add('in'); }); }
      else{ var io=new IntersectionObserver(function(es){ es.forEach(function(e){ if(!e.isIntersecting) return;
          e.target.classList.add('in'); io.unobserve(e.target); }); },{threshold:0,rootMargin:'0px 0px -5% 0px'});
        rises.forEach(function(e){ io.observe(e); }); }
    }catch(e){ [].forEach.call(RZT.all('.rise'),function(e){ e.classList.add('in'); }); }

    /* ---------- the basting stitches draw themselves. a clipped element never intersects,
         so an observer on it can never fire. driven off scroll position instead. ---------- */
    var DRAWS=[], drawRaf=0;
    try{ DRAWS=[].slice.call(RZT.all('.baste'));
         if(RM){ DRAWS.forEach(function(b){ b.classList.add('in'); }); DRAWS=[]; } }catch(e){}
    function drawPass(){ drawRaf=0; if(!DRAWS.length) return;
      var vh=window.innerHeight,i=DRAWS.length;
      while(i--){ var r=DRAWS[i].getBoundingClientRect();
        if(r.top<vh*.9&&r.bottom>0){ DRAWS[i].classList.add('in'); DRAWS.splice(i,1); } } }
    function drawReq(){ if(!drawRaf) drawRaf=requestAnimationFrame(drawPass); }
    try{ addEventListener('scroll',drawReq,{passive:true}); addEventListener('resize',drawReq,{passive:true}); drawReq(); }catch(e){}

    /* ---------- who he is. same contract as the sales page. ---------- */
    var YOU={};
    try{
      var st={}; try{ st=JSON.parse(localStorage.getItem('renz_you')||localStorage.getItem('renz_score')||'{}')||{}; }catch(e){ st={}; }
      function name(v){ v=(v||'').toString().trim().split(/\s+/)[0].replace(/[^A-Za-z\u00C0-\u00FF'\u2019\-]/g,'').slice(0,18);
        if(v.length<2) return ''; return v.toLowerCase().replace(/(^|[\-'\u2019])([a-z\u00E0-\u00FF])/g,function(m,x,y){ return x+y.toUpperCase(); }); }
      /* GoHighLevel's own merge field is {{contact.first_name}}, so the redirect after a
         booking naturally carries first_name. Taking it before anything in storage means
         the page greets the man who just booked, not whoever used this browser last. */
      YOU.name = name(qp('first_name')||qp('firstName')||qp('fn')||qp('name')||st.name);
      YOU.want = ({deals:1,respect:1,dates:1,easy:1}[qp('want')||st.want]) ? (qp('want')||st.want) : '';
      YOU.lid  = qp('lid')||st.lid||'';
    }catch(e){}

    /* ---------- his name, and the same order number he was given on the way in ---------- */
    try{
      if(YOU.name){
        /* This runs before the time has been read, so it cannot know whether he has booked.
           It only records his name. The calendar block below picks the sentence. */
        window.__rzName = YOU.name;
        $('doc-name').textContent=YOU.name;
      }
      var WANT={deals:'walk in believed',respect:'be taken seriously from the start',
                dates:'be the one people notice',easy:'never think about it again'}[YOU.want];
      if(WANT) $('sub').innerHTML='Most men mean to sort this out and never do. You just did. <b>Now we get you what you came for.</b>';
    }catch(e){}

    /* ---------- the booked slot, only ever said if the booking passed one ---------- */
    try{
      /* GoHighLevel names this differently depending on how the redirect is set up, and the
         name is easy to get wrong by one character. Rather than demand one exact spelling,
         take the first parameter that looks like a date we can read. */
      var WHEN_KEYS=['when','start_time','startTime','event_start_time','eventStartTime',
                     'appointment_start_time','appointmentStartTime','selected_slot','slot',
                     'start','date','datetime','appointment_time'];
      var when='';
      try{
        var qs=new URLSearchParams(location.search);
        for(var wi=0; wi<WHEN_KEYS.length && !when; wi++){
          var cand=qs.get(WHEN_KEYS[wi]);
          if(cand && !isNaN(new Date(cand).getTime())) when=cand;
        }
        /* last resort: any parameter at all whose value parses as a date with a year in it */
        if(!when){ qs.forEach(function(v){
          if(!when && v && /\d{4}/.test(v) && !isNaN(new Date(v).getTime())) when=v; }); }
      }catch(e){}
      if(when){ try{ localStorage.setItem('renz_when',when); }catch(e){} }
      else { try{ when=localStorage.getItem('renz_when')||''; }catch(e){} }
      /* ---------------------------------------------------------------- the calendar
         He has paid but he has not picked a time, so the booking widget goes on this page
         and he never leaves to do it. Once there IS a time, the calendar is replaced by
         the time, because offering a man a calendar he has already used is confusing. */
      (function(){
        var CAL  = 'https://api.leadconnectorhq.com/widget/booking/RYS7dx7Mc0qtjARUFxLP';
        var wrap = $('cal'), slot = $('slot');
        if(!wrap) return;

        var wasBooked = false;
        try{ wasBooked = localStorage.getItem('renz_booked') === '1'; }catch(e){}
        var booked = !!(when && !isNaN(new Date(when).getTime())) || wasBooked;
        var nm = window.__rzName ? ', ' + window.__rzName : '';

        /* A man who arrives with his time already picked was being told, four lines under
           "Your Call Is Booked", to go and pick a time, with a button that scrolled him to a
           calendar that is not on the page any more. Step one is done, so say it is done.
           The worklist wakes up after this, so the tick is handed over rather than clicked. */
        if(booked){
          window.__rzStepOneDone = true;
          try{ $('job-book-p').innerHTML = 'Your time is held, and it is written at the top of '
            + 'this page. <b>Now the other three.</b>'; }catch(e){}
        }

        /* Booked is a different sentence from paid. Telling a man his call is booked while
           he is still staring at an empty calendar is how he decides it is already done and
           closes the tab. */
        try{
          if(booked){
            $('h1').textContent = 'Your Call Is Booked' + nm + '.';
            $('lede').innerHTML = 'Your time is held.<br>We start on your numbers today.'
              + '<br><b>You have stopped putting it off.</b>';
          } else {
            $('h1').textContent = 'Right. You\u2019re In' + nm + '.';
            $('lede').innerHTML = 'The call is paid for.<br>One thing left, and it takes ten '
              + 'seconds.<br><b>Pick your time, just under this video.</b>';
          }
        }catch(e){}

        /* and the way back, wired once, whichever way he got here */
        var again = $('again');
        if(again){
          var link = $('again-a');
          if(link) link.addEventListener('click', function(e){
            e.preventDefault();
            try{ localStorage.removeItem('renz_booked'); }catch(e2){}
            try{ localStorage.removeItem('renz_when'); }catch(e2){}
            /* the page cannot go on saying his call is booked while he is stood in front of
               an open calendar looking for another one */
            try{
              $('h1').textContent = 'Pick Your New Time' + nm + '.';
              /* Booking again does not drop the first one, so do not let him think it does.
                 Either he cancels it from the email, or he messages Lorenzo, who moves it. */
              $('lede').innerHTML = 'Take whichever slot suits you better.'
                + '<br>Then drop the first one with the link in your confirmation email.'
                + '<br><b>Or message me and I\u2019ll move it for you.</b>';
            }catch(e2){}
            if(slot) slot.hidden = true;
            again.hidden = true;
            wrap.hidden = false;
            [].forEach.call(RZT.all('[data-tocal]'),function(a){ a.hidden = false; });
            try{ wrap.scrollIntoView({behavior:'smooth', block:'start'}); }catch(e2){ wrap.scrollIntoView(); }
            build();
          });
        }

        if(booked){
          wrap.hidden = true;
          if(slot) slot.hidden = false;
          if(again) again.hidden = false;
          /* the calendar has just left the page, so the button that scrolls to it goes too */
          [].forEach.call(RZT.all('[data-tocal]'),function(a){ a.hidden = true; });
          return;
        }
        if(slot) slot.hidden = true;
        wrap.hidden = false;
        build();

        /* Named and guarded, because the calendar is drawn either on arrival or later, when a
           man who has already booked asks for a different time. Twice would be two calendars. */
        function build(){
        if(wrap.querySelector('.cal-frame iframe')) return;

        /* carry across whatever we already know, so he is not typing it a third time */
        var url = CAL, q = [];
        try{
          var sp = new URLSearchParams(location.search);
          var st = {};
          try{ st = JSON.parse(localStorage.getItem('renz_score')||'{}')||{}; }catch(e){}
          var fn = sp.get('first_name')||sp.get('firstName')||sp.get('fn')||st.first_name||'';
          var ln = sp.get('last_name')||sp.get('lastName')||st.last_name||'';
          var em = sp.get('email')||st.email||'';
          if(fn) q.push('first_name='+encodeURIComponent(fn));
          if(ln) q.push('last_name='+encodeURIComponent(ln));
          if(em) q.push('email='+encodeURIComponent(em));
          if(q.length) url += (url.indexOf('?')>-1?'&':'?') + q.join('&');
        }catch(e){}

        var frame = document.createElement('iframe');
        frame.src = url;
        frame.title = 'Pick a time for your Style Solve call';
        frame.setAttribute('scrolling','no');
        frame.addEventListener('load', function(){
          var l = $('cal-load'); if(l) l.parentNode.removeChild(l);
        });
        var box = wrap.querySelector('.cal-frame');
        if(box) box.appendChild(frame);

        /* GoHighLevel resizes its own embed through this */
        if(!RZT.one('script[src*="form_embed.js"]')){
          var s2 = document.createElement('script');
          s2.src = 'https://link.msgsndr.com/js/form_embed.js';
          s2.async = true;
          document.head.appendChild(s2);
        }
        }

        /* the link inside step one */
        [].forEach.call(RZT.all('[data-tocal]'), function(a){
          if(booked){ a.hidden = true; return; }
          a.addEventListener('click', function(e){
            e.preventDefault();
            try{ wrap.scrollIntoView({behavior:'smooth', block:'start'}); }
            catch(e2){ wrap.scrollIntoView(); }
          });
        });

        /* He picks his time here, in the calendar on this page, so nothing reloads and no
           redirect fires. When the widget says it is done, this page has to stop asking him
           to pick a time and start telling him when it is. If the widget never says anything,
           nothing breaks: he ticks step one himself like the other three.

           The widget's own code posts ["msgsndr-booking-complete",{fingerprint,calendarId}]
           to its parent the moment an appointment is made, and that is the one we are really
           waiting for. The rest are there in case they rename it. The words are deliberately
           narrow: "appointment" on its own turns up in the widget's housekeeping messages,
           and acting on that would take the calendar away from a man who has not booked yet.

           Note what is NOT in that payload: the time. So the page says his call is booked and
           leaves the slot reading Confirmed, rather than inventing an hour he never picked. */
        var settled = false;
        window.addEventListener('message', function(ev){
          if(settled) return;
          if(!/leadconnectorhq|msgsndr/i.test(ev.origin||'')) return;
          var txt = '';
          try{ txt = typeof ev.data === 'string' ? ev.data : JSON.stringify(ev.data||''); }catch(e){ return; }
          if(!/msgsndr-booking-complete|booking[_-](?:success|complete)|appointment_booked|appointment_scheduled|\bbooked\b|\bscheduled\b/i.test(txt)) return;
          settled = true;

          var li = $('job-book');
          if(li){
            var box2 = li.querySelector('[data-tick]');
            if(box2 && box2.getAttribute('aria-checked') !== 'true') box2.click();
          }

          var found = '';
          try{
            var m = txt.match(/\d{4}-\d{2}-\d{2}[T ][\d:]{5,}(?:[.\d]*)?(?:Z|[+-]\d{2}:?\d{2})?/);
            if(m && !isNaN(new Date(m[0]).getTime())) found = m[0];
          }catch(e){}
          try{ localStorage.setItem('renz_booked','1'); }catch(e){}
          if(found){ try{ localStorage.setItem('renz_when', found); }catch(e){} }

          /* his own confirmation inside the widget gets a moment before we take it away */
          setTimeout(function(){
            try{
              $('h1').textContent = 'Your Call Is Booked' + nm + '.';
              $('lede').innerHTML = 'Your time is held.<br>We start on your numbers today.'
                + '<br><b>You have stopped putting it off.</b>';
            }catch(e){}
            try{
              if(found && window.__rzPaintTime) window.__rzPaintTime(new Date(found));
            }catch(e){}
            if(slot) slot.hidden = false;
            if(again) again.hidden = false;
            wrap.hidden = true;
            [].forEach.call(RZT.all('[data-tocal]'),function(a){ a.hidden = true; });
            try{ window.scrollTo(0,0); }catch(e){}
          }, 1400);
        }, false);
      })();

      /* Named, because a man who picks his time in the calendar on this page has to be
         able to see it written out without the page reloading under him. */
      function paintTime(d){
        if(!(d&&!isNaN(d.getTime()))) return false;
        var DAY=['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'],
            MON=['January','February','March','April','May','June','July','August','September','October','November','December'];
        var hh=d.getHours(), mm=d.getMinutes();
        var t=((hh%12)||12)+(mm?':'+('0'+mm).slice(-2):'')+(hh<12?'am':'pm');
        $('slot-val').innerHTML=DAY[d.getDay()]+' '+d.getDate()+' '+MON[d.getMonth()]+
          '<em>'+t+' &middot; forty five minutes, one to one</em>';
        /* a real calendar file, built in his browser, no server needed */
        var end=new Date(d.getTime()+45*60000);
        function z(n){ return ('0'+n).slice(-2); }
        function ics(x){ return x.getUTCFullYear()+z(x.getUTCMonth()+1)+z(x.getUTCDate())+'T'+z(x.getUTCHours())+z(x.getUTCMinutes())+'00Z'; }
        var TITLE='The Style Solve with Lorenzo';
        var NOTE='Forty five minutes, one to one. Somewhere quiet, camera on, and near your closet if you can.';
        var gcal='https://calendar.google.com/calendar/render?action=TEMPLATE'+
          '&text='+encodeURIComponent(TITLE)+
          '&dates='+ics(d)+'/'+ics(end)+
          '&details='+encodeURIComponent(NOTE);
        [].forEach.call(RZT.all('[data-cal]'),function(a){
          a.setAttribute('href',gcal); a.setAttribute('target','_blank'); a.setAttribute('rel','noopener');
          a.removeAttribute('data-inert'); a.hidden=false; });

        /* and the file, for a man who does not use Google. only offered where a save can actually happen. */
        var icsText=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//Renz Tailors//Style Solve//EN','BEGIN:VEVENT',
          'UID:'+(YOU.lid||'renz')+'@renztailors','DTSTAMP:'+ics(new Date()),'DTSTART:'+ics(d),'DTEND:'+ics(end),
          'SUMMARY:'+TITLE,'DESCRIPTION:'+NOTE,'END:VEVENT','END:VCALENDAR'].join('\r\n');
        var icsBtn=$('cal-file');
        if(icsBtn){
          if(window.claude&&window.claude.use){
            window.claude.use('downloads').then(function(dl){
              if(!dl) return;
              icsBtn.hidden=false;
              icsBtn.addEventListener('click',function(e){ e.preventDefault();
                dl.save({filename:'style-solve.ics',data:icsText}); });
            }).catch(function(){});
          } else {
            icsBtn.hidden=false;
            icsBtn.setAttribute('href','data:text/calendar;charset=utf-8,'+encodeURIComponent(icsText));
            icsBtn.setAttribute('download','style-solve.ics');
          }
        }
        return true;
      }
      /* Before he has picked a time there is no calendar entry to add, so the button had no
         address and a tap did nothing except tick step four off. It stays away until the slot
         is known, which is the moment it can actually do its job. */
      [].forEach.call(RZT.all('[data-cal]'),function(a){
        if(!/^https?:/.test(a.getAttribute('href')||'')){ a.setAttribute('data-inert','1'); a.hidden=true; } });
      window.__rzPaintTime = paintTime;
      paintTime(when?new Date(when):null);
      paintTicket();
    }catch(e){}

    /* ---------- the film only when it exists, and it takes what the first screen has left ---------- */
    try{
      var vid=$('vid'), ph=$('vidph');
      if(VIDEO.iframe||VIDEO.file){
        ph.remove();
        var el;
        if(VIDEO.iframe){ el=document.createElement('iframe'); el.src=VIDEO.iframe;
          el.allow='autoplay; fullscreen; picture-in-picture'; el.setAttribute('allowfullscreen',''); el.title='Three things before we speak'; }
        else { el=document.createElement('video'); el.src=VIDEO.file; el.controls=true; el.playsInline=true;
          el.setAttribute('playsinline',''); el.preload='metadata'; if(VIDEO.poster) el.poster=VIDEO.poster; }
        vid.appendChild(el);
      }
    }catch(e){}
    function fitHero(){
      try{
        var vid=$('vid'), slot=RZT.one('.slot');
        if(!vid||vid.hidden||!slot||!vid.parentNode) return;
        var vh=window.innerHeight, safety=14, col=vid.parentNode.clientWidth;
        for(var pass=0;pass<4;pass++){
          var bottom=Math.round(vid.getBoundingClientRect().bottom+window.pageYOffset);
          var cur=vid.offsetHeight||1, delta=bottom-(vh-safety);
          if(Math.abs(delta)<4) break;
          var want=Math.max(96,cur-delta);
          vid.style.setProperty('--vw-fit',Math.min(col,Math.round(want*16/9))+'px');
          vid.classList.add('fitted');
        }
      }catch(e){}
    }
    try{ fitHero(); addEventListener('resize',fitHero,{passive:true});
         addEventListener('orientationchange',function(){ setTimeout(fitHero,220); },{passive:true});
         if(document.fonts&&document.fonts.ready) document.fonts.ready.then(fitHero); }catch(e){}

    /* ---------- the worklist. it remembers, because a man comes back to this page ---------- */
    try{(function(){
      var KEY='renz_thanks', state={};
      try{ state=JSON.parse(localStorage.getItem(KEY)||'{}')||{}; }catch(e){ state={}; }
      if(window.__rzStepOneDone) state['job-book']=1;
      var jobs=[].slice.call(RZT.all('.jobs>li'));

      function save(){ try{ localStorage.setItem(KEY,JSON.stringify(state)); }catch(e){} }

      function paint(){
        var n=0;
        jobs.forEach(function(li){
          var on=!!state[li.id];
          li.classList.toggle('done',on);
          var box=li.querySelector('[data-tick]'); if(box) box.setAttribute('aria-checked',on?'true':'false');
          if(on) n++;
        });
        /* the denominator follows the list. it was pinned at three while four steps were on
           the page, so a man who finished them all was told "4 of three done, -1 to go". */
        var total=jobs.length||1;
        $('pg-n').textContent=n; $('pg-left').textContent=Math.max(0,total-n);
        var d=$('pg-d'); if(d) d.textContent=(['none','one','two','three','four','five','six'][total]||total);
        $('pg-bar').style.width=Math.round(n/total*100)+'%';
        [].forEach.call(RZT.all('#recap li'),function(li){
          li.classList.toggle('done',!!state[li.getAttribute('data-for')]); });

        /* the style guide fills the moment the scan is in. that is the whole point of step two. */
        var scan=!!state['job-scan'];
        [].forEach.call(RZT.all('#doc-rows li[data-need="scan"]'),function(li,i){
          li.classList.toggle('filled',scan);
          var b=li.querySelector('b');
          if(b) b.textContent = scan ? 'in' : (i>=3 ? 'waiting on the photos' : 'waiting on the scan');
          if(!RM) li.style.transitionDelay=(scan?i*90:0)+'ms';
        });
        var cap=$('doc-cap');
        if(cap) cap.innerHTML = scan
          ? 'Five of the eight are in. <b>The rest gets written on the call.</b>'
          : 'Nothing in it yet. <b>The scan is what starts it.</b>';
      }

      function toggle(li){
        state[li.id]=!state[li.id];
        if(!state[li.id]) delete state[li.id];
        save(); paint();
      }

      jobs.forEach(function(li){
        var top=li.querySelector('[data-open]'), box=li.querySelector('[data-tick]');
        top.addEventListener('click',function(e){
          if(e.target.closest('[data-tick]')) return;
          var open=li.classList.toggle('open');
          top.setAttribute('aria-expanded',open?'true':'false');
        });
        box.addEventListener('click',function(e){ e.stopPropagation(); toggle(li); });
        box.addEventListener('keydown',function(e){
          if(e.key===' '||e.key==='Enter'){ e.preventDefault(); e.stopPropagation(); toggle(li); } });
      });

      /* the one that matters most is open when he lands */
      var scanLi=$('job-scan');
      if(scanLi&&!state['job-scan']){ scanLi.classList.add('open');
        scanLi.querySelector('[data-open]').setAttribute('aria-expanded','true'); }

      /* Adding the call to his calendar is the one job left on this page that is still a
         button, so it is the one job that can still tick itself off. The email, the scan and
         The Fitting Room all happen in his inbox, where this page cannot see them, so he
         ticks those three himself. */
      [].forEach.call(RZT.all('[data-cal]'),function(a){
        a.addEventListener('click',function(){ if(a.hasAttribute('data-inert')) return;
          if(!state['job-cal']){ state['job-cal']=1; save(); paint(); } }); });

      paint();
    })();}catch(e){}
    })();

    })();
    unveil();
    /* A stage with a quiz on it keeps the host page covered until the man is through it,
       and asks for the handover itself. Everything else goes in as soon as it is safe. */
    if (!window.__RZ_DEFER_HOME) handover();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', start, {once:true});
  } else {
    start();
  }
})();
